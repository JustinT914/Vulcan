int incomingByte,result;

void setup() {
Serial.begin(115200);//From MATLAB
incomingByte = 0;
result = 0;
}

void loop() {
 /*   //reads next integer in Serial
    incomingByte = Serial.parseInt();

    if(incomingByte!=0) result=1;
    
    // say what you got:
    Serial.print("Message received: ");
    Serial.println(result, DEC);
    delay(250);
   */
}
